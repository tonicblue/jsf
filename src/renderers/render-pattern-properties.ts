import type { Frame } from "../frame";
import renderNotImplemented from "./render-not-implemented";

export default function renderPatternProperties ({ root, schema, pathStack }: Frame) {
  return renderNotImplemented('renderPatternProperties', { root, schema, pathStack });
}