import type { Frame } from "../frame";
import renderNotImplemented from "./render-not-implemented";

export default function renderPatternProperties (frame: Frame) {
  return renderNotImplemented('renderPatternProperties', frame);
}