import type { Frame } from "../frame";
import renderNotImplemented from "./render-not-implemented";

export default function renderThen ({ root, schema, pathStack }: Frame) {
  return renderNotImplemented('renderThen', { root, schema, pathStack });
}